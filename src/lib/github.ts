export async function getGitHubActivity(): Promise<string> {
    // Simulating an API call or just returning a static string for now
    // In a real app, this would fetch from GitHub API
    return "pushed to main 2h ago";
}
